# Archived legacy variants

This folder stores non-canonical artifacts that can cause release ambiguity if left in active paths.

- `index.html.new` (legacy alternate HTML variant)
- `diff.txt` (historical diff artifact)

Do not deploy from this folder.
