{
  "name": "gibson-caretaker-services",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:safe": "npm run verify:preflight && wrangler deploy && npm run verify:postdeploy",
    "verify:preflight": "node scripts/release/verify-release-profile.mjs preflight",
    "verify:postdeploy": "node scripts/release/verify-release-profile.mjs postdeploy",
    "set-phone": "node scripts/set-phone.mjs",
    "db:init": "wrangler d1 execute gibson-leads --remote --file=migrations/0001_init.sql"
  },
  "dependencies": {
    "mimetext": "^3.0.24"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
